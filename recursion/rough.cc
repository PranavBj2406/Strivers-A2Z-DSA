#include<bits/stdc++.h>
using namespace std;



vector<vector<string>> n_queens(int n)
{
    vector<vector<string>> result;
    vector<string> ds(n);
    string s(n,'.');
    for(int i=0;i<n;i++)
    {
        ds[i]=s;
    }
    return result;
}

int main()
{
return 0;
}