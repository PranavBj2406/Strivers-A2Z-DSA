#include<bits/stdc++.h>
using namespace std;

vector<int> mergearr(vector<int> arr1,vector<int> arr2)
{
    int n1=arr1.size();
    int n2=arr2.size();
    int Asize = n1+n2;
    int start1=0,start2=0;
    vector<int> ans;
    while(left<m)

    for(int i=0;i<n1;i++)
    {
        arr1[i]=ans[i];
    }
    for(int i=0;i<n2;i++)
    {
        arr2[i]=ans[i];
    }
}

int main()
{
    
return 0;
}