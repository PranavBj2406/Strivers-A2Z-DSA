#include<bits/stdc++.h>
using namespace std;
vector<int> nxt_permutation(vector<int> &nums)
{
    for(int i=0;i<nums.size();i++)
    {
        if(nums[i])
    }
    return nums;
}
int main()
{
return 0;
}