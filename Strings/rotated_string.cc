#include<bits/stdc++.h>
using namespace std;
//  s = "rotation", goal = "tionrota"

bool valid(string s,string goal)
{
    string ans="";
    int i=0;
    while(i<s.length())
    {
        
    }
}

int main()
{

}