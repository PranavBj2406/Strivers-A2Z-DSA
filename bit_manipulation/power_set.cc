#include<bits/stdc++.h>
using namespace std;


vector<vector<int>> power_set(vector<int> &arr)
{
    int n=arr.size();
    string bin;
    int i=0;
    while(i<pow(2,n))
    {
        
    }
}



int main()
{
return 0;
}